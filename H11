Update the existing Angular component logic for bulk request creation without changing the current structure. For each row in the FormArray, extract UMFR, MFR, and Correspondent Reference from selectedFields (dynamic keys). Apply priority: UMFR > MFR > Correspondent Reference. Use the first non-empty value to call getInstructionSheetByMfUmfrCorrespondentRef, passing only that value and others as null. If all values are empty, skip API call and mark the row as failed with message "Invalid MFR, UMFR, Correspondent Reference". For valid responses, use the returned Instruction Sheet to build the payload using existing methods (buildRequestKeyValue, buildCommonRequestKeyValue) and call createBulkRequest. Each row must be processed independently and results (success/failed) should be stored per row and displayed in the response table. Keep all existing UI, form structure, and services unchanged; only extend the onCopy() logic.
