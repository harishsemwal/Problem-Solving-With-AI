Implement logic to process each row of a dynamic Angular FormArray independently. From each row, extract UMFR, MFR, and Correspondent Reference from selectedFields and apply priority (UMFR → MFR → Correspondent Ref). Use the first non-empty value to call the getInstructionSheetByMfUmfrCorrespondentRef API (pass only one reference, others as null). If all values are empty, skip API call and mark the request as failed with message "Invalid MFR, UMFR, Correspondent Reference". For valid responses, use the returned Instruction Sheet to build payload using existing methods (buildRequestKeyValue, buildCommonRequestKeyValue) and call createBulkRequest. Maintain response per row (success/failed) and update UI accordingly. Do not modify existing structure; only extend logic minimally.
